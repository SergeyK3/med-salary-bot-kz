# Copy to telegram_bot/.env for local development only.
# Production injects this variable through systemd EnvironmentFile.
TELEGRAM_TOKEN=
# Optional locally; the systemd unit sets an absolute path under /run.
CURRENT_PARAMS_PATH=current_params.json
